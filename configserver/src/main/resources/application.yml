spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: ${GIT_URI}
          username: ${GIT_USER}
          password: ${GIT_ACCESS_TOKEN}
          clone-on-start: true
server:
  port: 8888
