spring:
  application:
    name: eureka

server:
  port: 8761

#THis is eureka server. So there is no need to register with itself
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false